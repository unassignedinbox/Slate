# M9 — denoiser and motion-vector re-enable

M9 restores the two deferred presentation/history features as default-on:

- `ReSTIRIntegratorConfiguration::Denoise = true`
- `ReSTIRIntegratorConfiguration::TemporalReprojection = true`

Project-Zero exposes explicit A/B controls without changing the default:

```text
Project-Zero.exe --scene materialgrid
Project-Zero.exe --scene materialgrid --no-denoise
Project-Zero.exe --scene materialgrid --no-reprojection
Project-Zero.exe --scene materialgrid --no-denoise --no-reprojection
```

`--no-denoise` keeps the running linear accumulation and bypasses the à-trous presentation pass. `--no-reprojection` keeps the same-pixel history path. Both switches are intended for converged-image comparisons; the second also resets the sampling history when changed through the live integrator setter.

## CPU render validation

The GPU is not available in this checkout, so M9 is validated with the same CPU-proof discipline as the other material milestones rather than by treating a source grep as a render. Run:

```text
bash Exhibits/Workbench/Materials/RunMaterialGridM9Cpu.sh 256 2 Projects/Project-Zero/Diagnostics
```

`MaterialGridM9CpuRender.cpp` includes the existing Slang CPU port (`MaterialEvaluation.slang`, `SlangCpuShim.h`, the BVH/NEE/MIS tracer, ACES and PNG writer), and renders the material-grid component. `MaterialGridMaterials.h` is shared by the Vulkan scene exporter and the CPU renderer, so all 20 unique material slots, glass/transmission, subsurface, unlit/emissive coverage, and the luminaire use the same authored records. The CPU geometry mirrors the exporter’s 24-ring/48-segment spheres and 5×4 layout.

The actual Project-Zero default is now `Showcase.gltf` with the grid appended to the original 100-object analytical field. `RayTracingSolver::ConstructShowcaseScene()` preserves the existing sun, sky, clouds, moon, stars, ground mist and lens-flare path, then appends the grid descriptors and geometry. The CPU reference for that combined default is rendered at high quality with:

```text
bash Projects/Project-Zero/RunHighQualityShowcaseCpu.sh 1280 720 12 4
```

The latest combined render completed successfully in 146.4 seconds at 1280×720. Authored primary and secondary rays use the exact `MaterialEvaluation.slang` BSDF for reflection/refraction, including smooth sphere normals, the grid luminaire, sky-backed transmission lookup and the CPU five-level normal/depth-aware à-trous presentation; it no longer uses the old flat Lambert fallback.

The command produces actual material A/B PNGs and a SHA-256 manifest, then runs the high-quality combined default Project-Zero reference render (`Projects/Project-Zero/Diagnostics/ProjectZero_Showcase.png`). That second render keeps the original scattered shapes, sun/sky/clouds, lens flare, moon/stars, ground mist, and CPU ReSTIR DI/GI path; the material A/B images remain the focused same-Slang material proof.

| Artifact | Meaning |
|---|---|
| `ProjectZero_MaterialGrid_raw.png` | current CPU-rendered accumulation |
| `ProjectZero_MaterialGrid_enabled.png` | temporal reprojection plus five-level edge-aware à-trous output |
| `ProjectZero_MaterialGrid_no_denoise.png` | reprojection A/B with denoise disabled |
| `ProjectZero_MaterialGrid_no_reprojection.png` | denoise A/B with reprojection disabled |
| `ProjectZero_MaterialGrid_ui.png` | selected output with the CPU-rasterized Project-Zero status presentation |
| `ProjectZero_MaterialGrid_M9.sha256` | hashes for all five images |

The CPU A/B uses a previous-camera frame, a bounded motion-vector lookup `(2, 1)` pixels, off-screen rejection, running two-sample history blending, and five à-trous levels. It therefore exercises rendered pixels for both enabled features, not merely their source wiring. The UI artifact includes the active `DENOISE` and `REPROJECT` state and finite-film metrics.

Reference run (`256×256`, `2 spp`, 2026-09-17):

```text
raw             8828bc3dbf1d4fa98a71537654616a61ba3d2fed4922e50596a318b926dc75ce
                mean=0.231646 energy=2.064399
enabled         1a3e84ba4ac31c2e50699bac49b4e41e3b088c749ce69f349bdb42ce3f845ed1
no_denoise      cba0214727171d74516b30290f8bd53ab9e3f0a916a66b5f3262c5516cba19e2
no_reprojection 704290907d6d1358eeb5ddc4e32d084f6576d9f60300711c9833aff7eed5357a
ui              9b26d167aa6e46e631a74e8e7056a82c3e5910a29535c0ff2bc2106cf1c41694
combined        139cc7e6485045e71363cf103ceaa0943afbfeb6e0e84b5ec1715272b613c247
                1280×720, 12 bounce candidates, 4 spatial passes, 146.4 seconds
```

The exact hash set is regenerated by the script after any CPU/compiler change. The PPM and build outputs under `Projects/Project-Zero/Diagnostics/` remain ignored; the PNG validation artifacts and SHA-256 manifests are checked in so the rendered result is visible in the repository.

## What is validated headlessly

`bash Exhibits/Workbench/Materials/CheckMaterialM9.sh` remains the source-contract and small CPU-behavior gate. It verifies the default-on feature bits, CLI wiring, raster motion convention, history normal/depth rejection, off-screen disocclusion, running-mean update, à-trous early-out/final-level writes, kernel-to-filter barriers, and the transmission/subsurface/sky source path. `RunMaterialGridM9Cpu.sh` is the complementary render gate: it compiles the Slang CPU counterpart, renders the Project-Zero scene, writes all A/B images, checks finite film values, and records hashes.

A real Vulkan device is still required for final GPU shader compilation and GPU-vs-CPU pixel agreement; that limitation is now isolated to the unavailable device-runner step. The CPU render is not reported as a GPU render result, but it is the required same-scene, same-material-math, actual-pixel validation available in this sandbox.
